#include <iostream>
#include <cmath>
using namespace std;

int main()
{   
    double x; 
    cout<<"Enter the number:"<<endl;
    cin>>x;
    cout<<"Square root of "<<x<<" is "<<sqrt(x);
    return 0;
}